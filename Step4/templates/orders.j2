<html>
<head>
    <title>Orders</title>
</head>
<body>
    <h1>Orders Page</h1>
    <p>This is the Orders page that shows the Orders' date, total price and the Customer ID that is associated with the order.</p>
    <h2>Navigation</h2>
    <p>[ <a href="/">Homepage</a> | <a href="/customers">Customers</a> | <a href="/orders">Orders</a> ]</p>
    <table border="1" cellpadding="5">
        <thread>
        <tr>
            <th>Date</th>
            <th>Customer ID</th>
        </tr>
        <tbody>
        {% for item in orders %}
        <tr>
            <td>{{item['Date']}}</td>
            <td>{{item['CustomerID']}}</td>
        </tr>
        {% endfor %}
    </table>
    <p>&nbsp;</p>
    <div id="insert">
        <form method="POST" id="addOrder">
            <legend><strong>Add Order</strong></legend>
                <fieldset class="fields">
                    <label> Date </label><input type="date" name="Date">
                    <label> Customer ID </label><input type="number" name="CustomerID">
                </fieldset>
                    <input class="btn" type="submit" id="addCustomer" value="Add Order">
                    <input class="btn" type="button" value="cancel" onClick="browseOrders()">
            </form>
    </div>
    <p>&nbsp;</p>
</body>
</html>
