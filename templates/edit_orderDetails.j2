<!-- Citation for the following: favicon -->
<!-- Date: 6.4.2024 -->
<!-- Downloaded from https://favicon.io/emoji-favicons/bookmark/ -->
<!-- Emoji is used for favicon, serves no purpose other than to look nice -->
<html>
<link rel="stylesheet" href={{url_for('static', filename='style.css')}}>
<link rel="apple-touch-icon" sizes="180x180" href={{url_for('static',filename="apple-touch-icon.png")}}> 
<link rel="icon" type="favicon/png" sizes="512x512" href={{url_for('static', filename="android-chrome-512x512.png")}}> 
<link rel="icon" type="favicon/png" sizes="192x192" href={{url_for('static', filename="android-chrome-192x192.png")}}> 
<link rel="icon" type="favicon/png" sizes="32x32" href={{url_for('static', filename="favicon-32x32.png")}}> 
<link rel="icon" type="favicon/png" sizes="16x16" href={{url_for('static', filename="favicon-16x16.png")}}> 
<head>
	<title>Edit Order Details</title>
</head>
<body>
    <nav>
    <a href="/">Home Page</a> 
    <a href="/books">Books</a> 
    <a href="/publishers">Publishers</a> 
    <a href="/authors">Authors</a>  
    <a href="/customers">Customers</a>
    <a href="/orders">Orders</a>  
    <a href="/orderdetails">Orders Details</a>
    </nav>
    <main>
    <h1>Order Details</h1>
    <table border="1" cellpadding="5"> 
        {% if data %}
            <thead>
                <tr>
                    <!-- Iterate through each key in the first entry to get the column name -->
                    {% for key in data[0].keys() %}
                    <!-- Create a <th> tag with the key inside of it, this will be our header row -->
                    <th scope="col" data-field="{{ key }}" data-filter-control="input">{{ key }}</th>
                    {% endfor %} 
                </tr>
            <tbody>
                <!-- Now, iterate through every item in data -->
                {% for item in data %}
                <tr>
                    <!-- Then iterate through every key in the current item dictionary -->
                    {% for key in item.keys() %}
                    <!-- Create a <td> element with the value of that key in it -->
                    <td>{{item[key]}}</td> {% endfor %} 
                </tr> 
                {% endfor %} 
        {% endif %} 
    </table>
    <p>&nbsp;</p>
    <form id="editOrderDetails" method="post">
        <legend><strong>Edit Order Details</strong></legend>
        <fieldset>
            <input type="hidden" value="{{data[0].ID}}" class="form-control" id="orderDetailsID" name="orderDetailsID" required>
            <label for="make">Title:</label>
            <select class="form-control" id="title" name="title" placeholder="" required> 
                <option value="0">&nbsp;</option>
                {% for title in titles %}
                    {% if title.ISBN == data[0].BookISBN %}
                        <option value="{{ title.ISBN }}" selected>{{title.Title}}</option>
                    {% else %}
                        <option value="{{ title.ISBN }}">{{title.Title}}</option>
                    {% endif %}    
                {% endfor %} 
            </select>	                    
            <label for="model">Order ID:</label>
            <input type="text" value="{{data[0].OID}}" class="form-control" id="OID" name="OID" pattern="[A-Za-z0-9]{1,}" required>
            <label for="color">OrderQty:</label>
            <input type="text" value="{{data[0].OrderQty}}" class="form-control" id="OrderQty" name="OrderQty" pattern="[A-Za-z0-9]{1,}"> 

            <input type="hidden" value="{{data[0].UnitPrice}}" class="form-control" id="UnitPrice" name="UnitPrice" required>
            <input type="hidden" value="{{data[0].LineTotal}}" class="form-control" id="LineTotal" name="LineTotal" required>
        
        </fieldset>
        <input type="submit" value="Edit Order Details" name="Edit_orderDetails" class="btn btn-primary">
        <a href='/orderdetails'><input class="btn" type="button" value="Cancel"></a>
    </form>
</main>
</body>   
<footer>
    <p>
    &copy; William Chen and Patrick Kim 2024
    </p>
</footer>     
</html>