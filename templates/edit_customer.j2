<!-- Citation for the following: favicon -->
<!-- Date: 6.4.2024 -->
<!-- Downloaded from https://favicon.io/emoji-favicons/bookmark/ -->
<!-- Emoji is used for favicon, serves no purpose other than to look nice -->

<html>
<link rel="stylesheet" href={{url_for('static', filename='style.css')}}>
<link rel="apple-touch-icon" sizes="180x180" href={{url_for('static',filename="apple-touch-icon.png")}}> 
<link rel="icon" type="favicon/png" sizes="512x512" href={{url_for('static', filename="android-chrome-512x512.png")}}> 
<link rel="icon" type="favicon/png" sizes="192x192" href={{url_for('static', filename="android-chrome-192x192.png")}}> 
<link rel="icon" type="favicon/png" sizes="32x32" href={{url_for('static', filename="favicon-32x32.png")}}> 
<link rel="icon" type="favicon/png" sizes="16x16" href={{url_for('static', filename="favicon-16x16.png")}}> 
<head>
	<title>Edit Customer</title>
</head>
<body>
    <nav>
    <a href="/">Home Page</a> 
    <a href="/books">Books</a> 
    <a href="/publishers">Publishers</a> 
    <a href="/authors">Authors</a>  
    <a href="/customers">Customers</a>
    <a href="/orders">Orders</a>  
    <a href="/orderdetails">Orders Details</a>
    </nav>
    <main>
    <h1>Customer</h1>
    <table border="1" cellpadding="5"> 
        {% if data %}
                <thead>
                    <th>
                        <strong>
                        Customer ID
                        </strong
                    </th>
                    <th>
                        <strong>
                        First Name
                        </strong
                    </th>
                    <th>
                        <strong>
                        Last Name
                        </strong
                    </th>
                    <th>
                        <strong>
                        Email
                        </strong
                    </th>
                </thead>
            <tbody>
                <!-- Now, iterate through every item in data -->
                {% for item in data %}
                <tr>
                    <!-- Then iterate through every key in the current item dictionary -->
                    {% for key in item.keys() %}
                    <!-- Create a <td> element with the value of that key in it -->
                    <td>{{item[key]}}</td> {% endfor %} 
                </tr> 
                {% endfor %} 
        {% endif %} 
    </table>
    <p>&nbsp;</p>
    <form method="post">
        <legend><strong>Edit Customer</strong></legend>
        <fieldset>
            <input type="hidden" value="{{data[0].ID}}" class="form-control" id="ID" name="ID" required>
            <label for="fname">First Name:</label>
            <input type="text" value="{{data[0].FirstName}}" id="fname" name="fname" placeholder="" required></input>	
            <label for="lname">Last Name:</label>
            <input type="text" value="{{data[0].LastName}}"  id="lname" name="lname" placeholder="" required></input>       
            <label for="email">Email:</label>
            <input type="email" value="{{data[0].Email}}" class="form-control" id="email" name="email" required>
        </fieldset>
        <input type="submit" value="Edit Customer" name="Edit_Customer" class="btn btn-primary">
        <a href='/customers'><input class="btn" type="button" value="Cancel"></a>
    </form>
</main>
</body>   
<footer>
    <p>
    &copy; William Chen and Patrick Kim 2024
    </p>
</footer>     
</html>